# PRDs

Product requirement documents, one per feature, matching the entries in `product-development/feature-index.yaml`.

*(empty: create a folder per feature as you scope it, e.g. `my-feature/prd.md`)*

Use `/write-spec` (product-management plugin skill) to fill these in properly once you're ready to scope a feature for real.
